# `archlinux-wallpaper`

The `archlinux-wallpaer` package contains Arch Linux wallpapers.

```sh
pacman -S archlinux-wallpaper
```

The wallpapers are stored under `/usr/share/backgrounds/archlinux/`.

---
