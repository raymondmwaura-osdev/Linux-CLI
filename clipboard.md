# Clipboard

Use `wl-clipboard` in wayland.

```sh
pacman -S wl-clipboard
```

You can then use `wl-copy` and `wl-paste` to copy and paste things.

```sh
cat file.txt | wl-copy # Copy file contents to clipboard.
```

---
