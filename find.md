# `find`

Use the find command to quickly find files or directories.

```
find /mnt/data -name school_project -type d
```

This will list the path to every directory named `school_project` in `/mnt/data` recursively.

---
